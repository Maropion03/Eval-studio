import Placeholder from './_Placeholder';

export default function Settings() {
    return (
        <Placeholder
            code="04 · SETTINGS"
            title="SETTINGS"
            subtitle="BYOK — your API keys never leave this machine. Compatible with SiliconFlow / OpenAI / DeepSeek / Moonshot and any OpenAI-compatible endpoint."
        />
    );
}
